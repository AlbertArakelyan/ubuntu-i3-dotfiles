# ubuntu-i3-dotfiles
Dotfiles, packages and commands of my Ubuntu i3 setup.

---

#### Installation command for Ubuntu (Kubuntu):
```bash
sudo apt update && sudo apt install i3-wm i3status i3lock dmenu xorg xinit x11-xserver-utils xterm alacritty feh picom
```

---
