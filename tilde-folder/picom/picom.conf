backend = "glx";

shadow = true;
shadow-radius = 12;
shadow-opacity = 0.3;

opacity-rule = [
  "92:class_g = 'Alacritty'",
  # "88:class_g = 'Rofi'"
];

fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;

# inactive-opacity = 0.7;

corner-radius = 12;
